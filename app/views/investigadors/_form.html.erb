<%= form_with(model: investigador, local: true, html: { class: 'form' }) do |form| %>
  <% if investigador.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(investigador.errors.count, "error") %> prohibited this investigador from being saved:</h2>

      <ul>
        <% investigador.errors.full_messages.each do |error_message| %>
          <li><%= error_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :nombre, class: 'form-label' %>
    <%= form.text_field :nombre, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.submit 'Create', class: 'btn btn-primary' %>
  </div>
<% end %>
